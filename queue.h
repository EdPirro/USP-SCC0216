/*typedef int elem;

typedef struct node {
	elem info;
	struct node* next;
} NodeQ;

typedef struct {
	NodeQ* begin;
	NodeQ* end;
} Queue;

void createQueue(Queue*);
int isQueueEmpty(Queue);
int pushToQueue(Queue*, elem);
int popFromQueue(Queue*, elem*);
int prinfFila(Queue q);*/
